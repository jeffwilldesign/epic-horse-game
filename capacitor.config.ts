import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'horse-game-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
